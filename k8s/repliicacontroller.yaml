apiVersion: v1
kind: ReplicationController
metadata:
  labels:
    app: nginx
  name: prashanth
spec:
  minReadySeconds: 15
  replicas: 5
  selector:
    app: nginx
  template:  
    metadata:
      labels:
        app: nginx
      name: prash
    spec:
      containers:
        - image: jenkins
          name: prash1
          ports:
            - containerPort: 8080
              protocol: TCP    